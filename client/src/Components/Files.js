import React, { useEffect, setData } from "react";

const Files = () => {
  // useEffect(() => {
  //   fetch("/uploads")
  //     .then((res) => res.json())
  //     .then((data) => {
  //       setData(data);
  //       console.log(data);
  //     });
  // }, []);

  return <main>List returned files here from API endpoint</main>;
};

export default Files;
